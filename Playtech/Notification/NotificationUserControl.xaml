<UserControl x:Class="Playtech.Notification.NotificationUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Playtech.Notification" d:DataContext="{d:DesignInstance Type=local:NotificationViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Visibility="{Binding IsNotificationVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
    <Viewbox>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Notification" Foreground="White" FontSize="16" FontWeight="Bold" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,5,0,0">
            <TextBlock Text="Player " Foreground="White" />
            <Border Background="#6A5ACD" CornerRadius="3" Padding="5,2">
                <TextBlock Text="VIP" Foreground="White" FontWeight="Bold" />
            </Border>
            <TextBlock Text=" PlayerName has joined the table." Foreground="White" />
        </StackPanel>
        </Grid>
    </Viewbox>
</UserControl>
